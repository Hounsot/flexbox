{
  "name": "parametr-flex",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://hounsot.github.io/flexbox",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^6.0.3"
  }
}
